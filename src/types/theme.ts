export enum THEMES {
    light = "light",
    dark = "dark",
}

export enum BUTTON_VARIANTS {
    default = "default",
    secondary = "secondary",
    destructive = "destructive",
    link = "link",
    ghost = "ghost",
}